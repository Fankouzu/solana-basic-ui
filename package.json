{
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "deploy": "vitepress build docs && npx surge ./docs/.vitepress/dist solana-basic.surge.sh"
  },
  "devDependencies": {
    "surge": "^0.23.1",
    "vitepress": "latest"
  }
}
